

1. package.json 中的依赖包并不能直接安装，会报错，需要用 node_modules.7z 里的依赖，直接放进去

2.  /src/main/Main.ts  win = new BrowserWindow 下方增加 win.webContents.openDevTools({mode:'right'}) 以打开调试

3. 代码有变化需要先执行打包，再运行才能生效